var language = 'JavaScript';
var idx = language.indexOf('S');
var charCode = language.charCodeAt(idx);
var string = String.fromCharCode(charCode);
var lastIndex = language.lastIndexOf('a');
console.log(lastIndex);
